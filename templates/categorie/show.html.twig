{% extends 'base.html.twig' %}

{% block title %}
    Categorie
    
{% endblock %}

{% block body %}
    <h1 class="text-5xl font-bold p-20">
        Categorie     
    </h1>
    <div class="overflow-x-auto pl-20 pr-20 pb-20">
        <table class="table">
            <tbody>
                <tr>
                    <th>
                        Id
                                        
                    </th>
                    <td>
                        {{ categorie.id }}
                    </td>
                </tr>
                <tr>
                    <th>
                        Name
                                        
                    </th>
                    <td>
                        {{ categorie.name }}
                    </td>
                </tr>
                <tr>
                    <th>
                        Actions
                                        
                    </th>
                    <td class="px-4 py-2" style="display:flex;flex-direction:row;justify-content:space-between;">
                        <a class="btn btn-outline btn-secondary" href="{{ path('app_categorie_index') }}">
                            back to list
                                                
                        </a>

                        <a class="btn btn-outline btn-primary" href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">
                            edit
                                                
                        </a>

                        {{ include('categorie/_delete_form.html.twig') }}
                    </td>
                    <tr></tbody>
                </table>
            </div>
        {% endblock %}

    